# Pending updates — Power Price Data

## 🔴 OPEN COMMITMENTS

Cross-session commitments that outlive a session. Read by the `open-commitments.sh` SessionStart hook
and the cross-session task view.

**Format is load-bearing:** exactly four columns, no `|` inside a cell, and a status cell that starts
with `done` / `recorded` / `accepted` / `closed` to drop off the list.

| # | Item | Status | Blocked on |
|---|---|---|---|
| 1 | Wire the 6 PowerQuery queries on the Windows work machine | done 2026-07-30 — was already complete; verified in the shipped workbook: 19 connections incl. `g1_solar_peakhour` + `g2_price_by_month`, each with queryTable + table + load-target tab (`G1_SolarPeak`, `G2_MonthDuck` present). `WORK_MACHINE_SETUP.md` no longer documents wiring at all (commit `b981ee5`) | Nothing |
| 2 | Push the deliberately-local follow-up commit — `config.py` DISPLAY_END_YEAR 2035→2030 plus untracked deck-builder scripts | closed 2026-07-30 — premise false on all three counts. `HEAD == origin/main` so nothing was unpushed; DISPLAY_END_YEAR is 2035 and was never 2030 in any commit; all six scripts are tracked. The change was recorded as made but never made | Nothing |
| 3 | Capture-price-vs-baseload-by-technology chart renders with transparent or empty white bars | done 2026-07-31 — THREE causes, and the first two were not what Fred meant. The six leftover technologies were fixed 2026-07-22 (`289a352`). German Nuclear was moved to the TAIL of the DE block (2026-07-30) so Fig 5 takes a 10-row prefix while Fig 9 keeps all 11. But the ACTUAL reported defect was neither: every bar series carried invertIfNegative=1, so every NEGATIVE bar drew as a hollow white outline — Solar, Onshore and Offshore wind, the whole point of the exhibit. A rendering flag, invisible to every data check and to the PNG path, found only by opening the workbook and looking. Fixed across 34 series in 5 charts and verified on screen | Nothing |
| 4 | Decide DISPLAY_END_YEAR 2035 vs 2030 — a real open question, previously mis-recorded as settled. Changing it reshapes the trailing blank columns in the 12 already-wired CSVs, so it is not a tidy-up. NOTE the reserved columns are load-bearing — READ_ME_FIRST tells the reader "charts are pre-wired and pre-allocated to 2035, so future years fill in automatically on refresh" — so shortening the runway to 2030 buys tidier CSVs at the cost of a 2031 cliff. Leaning KEEP 2035 | closed 2026-07-31 — Fred chose KEEP 2035. The reserved columns are load-bearing (READ_ME_FIRST promises future years fill in on refresh), the rolling window makes the extra width harmless, and 2030 would buy tidier CSVs at the cost of a 2031 cliff plus a table-shape change — the class of edit that has bitten this project twice | Nothing |
| 5 | Confirm the monthly refresh actually fires unattended. Every run to date was a manual `workflow_dispatch`; the schedule has never once triggered by itself. Twice corrected: this row originally said cron `0 6 2 * *` firing 2026-08-02 06:00, which was already wrong when written (moved to `23 7 3 * *` in `5d9d673`, same day). Then on 2026-08-01 Fred moved it to **`23 7 2 * *`** after the 1st-vs-2nd-vs-3rd analysis. First unattended firing is **2026-08-02 07:23 UTC** | done 2026-08-02 — it fired unattended. Run `30741717143`, event `schedule`, queued 09:24 UTC (about 2h behind the 07:23 cron, normal for shared runners), conclusion success. All eight jobs green — five country fetches, build, validate, publish. The new `check_month_arrived` coverage gate passed on its first live run: July 2026 is populated across all five markets in `figA_monthly_price.csv` (DE 105.47, ES 104.88, PT 107.11, FR 95.33, IT 157.16) and bot commit `0159633` "data refresh 2026-08-02" is on main | Nothing |
| 7 | done 2026-07-31 — verified: check_no_future_data reports 0 errors on published/. ~~BUG — the Excel G1 chart plotted 2.5 months into the future.~~ `extra_summaries.g1` builds `_qavg` with `groupby(quarter).transform("mean")`, which broadcasts a quarter's mean to EVERY day of that quarter. Coverage ends 2026-07-16, so Q3-2026's mean — computed from 16 days — is written to all 92 days, through 2026-09-30, and `chart14` plots the raw column. `render_all.py:159` clips the PNG path (`df[df["date"] <= QEND]`) but nothing clips the PUBLISHED CSV that Excel loads, so the two update paths disagree — the exact thing this project set out to prevent. `check_consistency` misses it because it compares chart structure, not data cutoffs | done — clipped in `extra_summaries.g1`; `check_consistency.check_no_future_data` now gates it | Nothing |
| 6 | done 2026-07-31 — verified in the shipped workbook: no setup steps remain, status-page link present, seen on screen. ~~The workbook's `READ_ME_FIRST` tab is STALE and actively misleading — it still walks the reader through "add ONE Power Query … Get Data > From Web …" for queries that are all already wired. Same false premise as row 1, but baked into the file Fred actually opens. It comes from the rebuild base `archive/phase4_2026-07-17/HourlyPowerData_pre-phase4.xlsx`, so fixing it meant a generate.py step (`fix_readme_sheet.py`).~~ | done | Nothing |
| 8 | done 2026-07-31 — the Windows Open XML job ran AFTER the build job committed, so an invalid package landed in `deliverables/` and only then failed the run. Restructured: `build` now commits nothing and hands everything on as the `publish-payload` artifact; `validate` gates on it; a new `publish` job is the only writer to the repo and runs last. Nothing reaches main that either the Open XML validator or the coverage guard rejected | done | Nothing |
| 9 | done 2026-07-31 — RESTORED, because this row was itself the evidence. Row 8 was added at 13:16 in `ec6a3c7` and deleted 30 minutes later by bot commit `8db1093`, which went green. Cause: the commit step used `git reset --soft origin/main`, which moves HEAD but leaves the INDEX holding the 40-minute-old checkout, so `git commit` wrote back every file that had changed on main during the run. It is now `--mixed`, so the index is rebuilt from origin and `git add` stages only the publish set, plus a guard that fails the run if anything outside that set is staged. Reproduced both behaviours against a local clone: `--soft` reverts the row, `--mixed` leaves it and still publishes the data | done | Nothing |
| 10 | done 2026-07-31 — coverage may no longer SHRINK between runs. `_tools/check_coverage.py` compares every published feed against the previous commit on TWO measures: row count, and populated cells PER COLUMN. Row count alone would have missed the bug that motivated it — the wide chart CSVs Excel loads are fixed-shape (366 day-of-year rows, 25 year rows), so a coverage collapse blanks cells and never moves a row; the 2026-07-31 refresh commits show 595 added / 595 deleted, identical totals. Rollover-aware: it reads the slot labels from `status.csv` and compares window column `w_k` against its previous position, without which the January 2027 run would have thrown 19 false failures as slot 8 reset from a complete year to a two-day one. Replayed over all 24 published transitions in the repo's history: silent on every ordinary refresh, and it fires on the commits that shipped bad data | done | Nothing |
| 11 | done 2026-07-31 — the coverage gate caught a REAL bug on its first live run, which is the entry worth keeping. Run 30648716543 was green through fetch, build and Open XML validation; the gate refused to publish because France and Italy had lost January–June 2026 from `capture_monthly` entirely — six months of capture prices — while DE/ES/PT were untouched. Cause, from the run's own fetch log: `FR_generation_2026.parquet: column set changed — replacing wholesale` / `generation (20213 -> 2880 rows)`. ENTSO-E returns one column per generation type that actually reported, so a technology producing nothing in the trailing 30-day window is simply absent, which changed the column set — and `_merge_into` treated ANY column-set difference as a schema change and returned the window, discarding the stored year. Same hit FR and DE `flow_import`. Fixed: the merge now takes the UNION of columns, and asserts it may never return fewer rows than it was given | done | Nothing |
| 12 | The status page and the workbook both still advertise the WRONG DAY. The cron moved to `23 7 2 * *` on 2026-08-01 but `_tools/refresh-page/worker.js` still hardcodes `CRON = { day: 3 }` (its comment still cites the old `23 7 3 * *`) and `add_status_sheet.py` still writes "A job re-publishes the figures on the 3rd of each month" into the sheet the reader opens on. Checked live 2026-08-03: the page announced the next update as 3 Aug 07:23 UTC "in 26 minutes" — it is 2 Sept. For a non-technical reader whose only instrument is that page, a promised run that never arrives is the wrong signal, and it misfires every month. Fix is two lines plus a `wrangler deploy` and a `generate.py` rebuild | done 2026-08-03 — deployed and verified live. Fred ran `wrangler login && wrangler deploy`; version ec871866-5460-470a-ad52-9d1feb0cf651. The page now reads "Next scheduled update 10 Aug 2026 07:23 UTC" and "Runs automatically every week", and every stale "3rd of each month" / "monthly run" string is gone. nextRun() derives the next firing from BOTH cron lines and takes whichever is sooner, swept over all 17520 hourly points of 2026-2027 with zero failures including the year-end and February boundaries. The workbook and doc wording were corrected in the same push and ship on the next CI rebuild. ⚠ The Worker cannot see the workflow file, so if the cron ever changes again this constant must be changed by hand — that decoupling is what caused the original defect | Nothing |
| 13 | The status page's Refresh button will probably stop working during any long absence. It is the only self-service recovery lever a non-technical successor has, and it authenticates with a FINE-GRAINED GitHub PAT held as the Cloudflare Worker secret `GH_TOKEN` (Actions:write on this repo only). Fine-grained PATs expire — GitHub's default is 30 days, maximum 366. The expiry is readable ONLY at https://github.com/settings/personal-access-tokens: it is not exposed by the Worker, not returned by any API, and the Chrome automation profile is not signed in to GitHub. If it lapses the page silently degrades to "Refresh is not enabled: no access token configured" and nobody is told. Reissue at maximum lifetime, then `wrangler secret put GH_TOKEN` | done 2026-08-03 — Fred read it off the GitHub settings page: expires Thu 15 July 2027. That is roughly eleven months out and comfortably outlasts any three-month absence, so the risk is closed rather than mitigated. Worth re-reading before any absence that would run past mid-2027, since nothing warns when it lapses — the page just silently stops offering the button | Nothing |
| 14 | One failed run may switch the monthly schedule off permanently. GitHub disables scheduled workflows after 60 days of repository inactivity. The workflow comment assumes TWO consecutive failures are needed to reach that; the arithmetic says one is enough — last commit 2 Aug, so a failed 2 Sept run leaves no commit until 2 Oct, which is 61 days, and the schedule would be disabled around 1 Oct, the day before the run that would have healed it. The harder version: commits pushed by the Actions bot with `GITHUB_TOKEN` are widely reported not to reset the inactivity clock at all, in which case the schedule dies in early October regardless of whether anything failed. Not settleable by reading the code. Cheap insurance is a keepalive commit on a separate fortnightly cron, or an external trigger | done 2026-08-03 — Fred chose a weekly refresh over a keepalive commit, which is the stronger fix: it removes the inactivity question entirely rather than papering over it, and cuts recovery from a failed run from thirty days to seven. Cron is now `23 7 * * 1` plus the original `23 7 2 * *`; the monthly line is kept because it is what actually lands the just-closed month, and the 24-hour grace in check_month_arrived means the extra weekly runs cannot fail spuriously at a month boundary. Staleness tolerance cut 45 days to 14 to match. Note the cache side-effect, documented in the workflow: weekly runs sit ON the ~7-day eviction boundary so they will often find a warm cache and be genuinely incremental, where monthly runs always found it cold and re-pulled the year | Nothing |
| 15 | Dependencies float, so a major release lands unreviewed into a build nobody is watching. Only `entsoe-py` is pinned; `pandas>=2.0`, `pyarrow>=15`, `duckdb>=1.0`, `matplotlib>=3.8`, `openpyxl>=3.1`, `python-pptx>=0.6.23` and `lxml>=5.0` are all open-ended and every CI job does a fresh `pip install -r requirements.txt`. A pandas 3.0 landing mid-absence is the obvious candidate to break the monthly run | done 2026-08-03 — and the audit UNDERSTATED this. It called pandas 3.0 a future risk; the 2026-08-02 CI log shows the resolver had ALREADY walked to pandas 3.0.5, pyarrow 25.0.0 and duckdb 1.5.5, three unreviewed majors that came up green by luck and were visible only by reading a CI log after the fact. Now pinned exactly to that verified-green set. Exact rather than capped ranges because of the SHAPE of the failure: a stale pin fails loudly at pip install and publishes nothing, while a drifting version can change a groupby default, publish, and pass the coverage guard since the data is the right shape and merely the wrong numbers | Nothing |
| 16 | Nobody is told when a run goes red. GitHub emails the repository OWNER only, so during an absence the alert goes to the one person who is uncontactable. A successor learns of a failure solely through the workbook's 45-day staleness tolerance, so a 2 Sept failure first surfaces around 16 Sept. That is survivable, but it is the actual mechanism and any handover document has to say so rather than implying someone is watching. There is also no named escalation contact with repository access — that gap is a person, not a document | OPEN — Fred decided 2026-08-03 that the handover pack should assume NOBODY technical is available, so it must be built purely around self-service (status page, refresh button, download links) and state honestly what cannot be recovered without a developer. That settles how to write it; the underlying exposure is unchanged and is now a known, accepted risk rather than an open question | Nothing — decided, pending the pack being written |
| 17 | Settle the run cadence and the fetch mode, after the move off monthly-only. Fred asked whether every run could be identical to the monthly one, and whether it could be four runs per month rather than necessarily weekly | done 2026-08-03 — both, and the reasoning is worth keeping. Cron is `23 7 2,10,18,26 * *`: exactly four runs in every month on the same dates, gaps 8/8/8 then 7 at the wrap and less in February, verified by enumeration over 2026-2028 at 48 runs a year with a 4-day minimum and 8-day maximum. Fixed dates beat a weekday cron on explainability — the dates are a complete answer to when it runs, where weekly gives four runs in some months and five in others. EVERY run now re-pulls the whole year with --force; the incremental path and the GitHub raw cache are DELETED. The cache's value rested on a coincidence — ~7-day eviction meant a monthly schedule always found it cold and rebuilt the year, a self-repair nobody designed — and running more often would have kept it warm and silently ended that. One mode costs ~30 min a run against ~5, free on a public repo and inside the 100-minute timeout, and repairs a bad stored file within 8 days. Staleness tolerance set to 10, deliberately NOT the 8-day cadence, because GitHub starts scheduled jobs late (measured 2h02m) and a threshold equal to the gap would cry wolf before every ordinary run. Worker nextRun rewritten and swept over 26304 hourly points of 2026-2028 with zero failures; deployed as version c9ba7b8c-a54e-4f7a-bdb0-84ff325cbf78 and the live page verified reading the new dates | Nothing |
| 18 | Nothing announced a failed run. GitHub emails the repository owner, but that is a per-account setting nobody can verify from outside the account, it goes to one address, and it is easily filtered away | done 2026-08-03 — a `notify` job now opens a GitHub ISSUE on any failure, deliberately not an email: it is durable and sits in the Issues tab until closed, @-mentions the owner so it notifies regardless of the watch setting, carries a direct link to the failed run log, and needs no secret, mail service or external account, so there is nothing in the channel that can expire while nobody is looking. One open issue is reused across failures with a comment per run. The label is created BEFORE it is used as a filter, because `gh issue list --label` errors on a missing label and under `set -e` that would have killed the very job whose purpose is to raise the alarm. Body and both branches verified locally against a stubbed gh. NOT yet proven end-to-end against real GitHub permissions — that needs a deliberately failing run | Nothing |

<!-- append entries below -->

2026-07-30 · **Queue created; orphaned session `9f9ca682` (quiet since 17 Jul) retired into it.**
⚠ **A first version of this file, written earlier the same day, was WRONG** — it recorded the
"REMAINING (a)(b)(c)" block from `current-status.md` (country-variant charts, deck slides, validate)
as open. Those three were completed the same evening (2026-07-17), by a **DONE block sitting a few
lines further down the same file**, and the project has since moved through Phase 6 to charts 1–19.
Reading the "REMAINING" heading without reading past it is precisely the stale-status trap this
vault has been bitten by before. Rows 1–3 above are the genuine state as of the 2026-07-21 update.

2026-07-30 (later) · **Rows 1 and 2 were ALSO wrong, and are now closed.** The correction above fixed
which items were listed but not whether they were real — both had already been overtaken, and row 2's
premise never existed. Verified directly against the workbook and the repo, not against a status file:
row 1's 19 queries are all wired; row 2's unpushed commit, config change and untracked scripts are all
absent. Rows 4–6 replace them with what is genuinely outstanding.

**The pattern worth naming:** three successive queues for this project were each built by reading a
status document rather than the artefact it described. `current-status.md` claimed the
DISPLAY_END_YEAR change was made (twice), and that Fred still had queries to wire; neither was true.
A status file is a claim. Check the workbook, the CSV, or `git log` before putting an item on a list —
and before taking one off.
