name = "power-price-data"
main = "worker.js"
compatibility_date = "2026-07-30"

# A Worker, NOT a Pages deploy — deliberately. The `fredhill` Pages project is gated by
# Cloudflare Access on `fredhill.pages.dev` only. This line said "and `*.fredhill.pages.dev`"
# until 2026-08-17, which was false: every Pages deployment alias was open to anyone, measured
# that day (see `_meta/cloudflare-pages-hosting.md`). This page has to be
# public, and Workers live on a separate product and a separate hostname
# (`power-price-data.<subdomain>.workers.dev`), so deploying here cannot touch that gate.
workers_dev = true

# The GitHub token is a SECRET, never a var — set it out of band with:
#   wrangler secret put GH_TOKEN
# Fine-grained, Actions:write on Power-Utilities-team/power-price-data only.
